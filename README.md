## Firewall-Simulator
This is a firewall simulator made with Python. Using the "ipaddress" module, we can define our network ranges we want to allow or deny.

We then generate random IP's that are checked against our "firewall rules", then generates an IP address, what action the firewall has taken, and a OID to go with.
